# This GitHub Actions workflow was auto-generated by the `rdme` cli on 2026-01-19T16:59:45.283Z
# You can view our full documentation here: https://docs.readme.com/docs/rdme
name: ReadMe GitHub Action 🦉

on:
  push:
    branches:
      # This workflow will run every time you push code to the following branch: `main`
      # Check out GitHub's docs for more info on configuring this:
      # https://docs.github.com/actions/using-workflows/events-that-trigger-workflows
      - main

jobs:
  rdme-openapi:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo 📚
        uses: actions/checkout@v4
      - name: Find and Replace
        uses: jacobtomlinson/gha-find-replace@master
        with:
          find: "Unused"
          replace: "Authorization"
          include: "terraform/upload-service.yml"
      - name: Run `openapi` command 🚀
        uses: readmeio/rdme@v9
        with:
          rdme: openapi terraform/upload-service.yml --key=${{ secrets.README_API_KEY }} --id=696e62f6b1190fbb558a0bb0
